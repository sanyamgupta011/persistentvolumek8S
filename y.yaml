apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: demo-pvc
spec:
  storageClassName: "standard"
  volumeName: "demo-pv"      
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

